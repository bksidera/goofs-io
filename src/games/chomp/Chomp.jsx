import GameScreen from './screens/GameScreen.jsx';
import './Chomp.css';

// Root component — single screen; the canvas handles title/game/gameover.
export default function Chomp() {
  return <GameScreen />;
}
